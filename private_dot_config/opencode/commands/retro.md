---
description: Review this session for workflow and agent improvements
agent: retrospective
---

Review the current session. Follow the procedure defined in your system prompt:

1. Call `agent_attribution` to get a per-message map of which agent authored each message. Use this to attribute observations, friction, and corrections to the correct agent.
2. Summarize what was accomplished.
3. Identify observations — what went well, what caused friction.
4. Propose specific changes to AGENTS.md or agent definition files, ordered by impact. Look for opportunities to **remove or condense** existing content — not just content to add. Identify material that is redundant, verbose, or no longer needed.
5. Suggest new commands, tools, plugins, subagents, or agents if session patterns warrant it. Suggest modifications to existing ones if they could be improved. Suggest removing or condensing existing ones if they are not useful or are redundant.

Focus on actionable improvements with exact proposed text, not vague suggestions.
